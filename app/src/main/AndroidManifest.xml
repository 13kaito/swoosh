<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.kk.swoosh">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_nameballer"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".Contrtoller.FinishActivity">
        </activity>
        <activity android:name=".Contrtoller.SkillActivity">
        </activity>
        <activity android:name=".Contrtoller.BaseActivity">
        </activity>
        <activity android:name=".Contrtoller.LeagueActivity">
        </activity>
        <activity android:name=".Contrtoller.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data
                android:name="preloaded_fonts"
                android:resource="@array/preloaded_fonts"/>
    </application>

</manifest>